#include <iostream>
#include <cstring>
#include <cctype>

using namespace std;

int main() {
	char s[] = "mary";
	char *p = s;
	// cout << s << endl;
	// cout << p << endl;

	// cout << strlen(s) << endl;

	// append " had" to s
	// strcat(s, " had");
	// cout << s << endl;

	// copies s to s
	//	* does not work if t's size is not previously declared
	char t[10];
	// strcpy(t, s);
	// cout << t << endl;

	// char river[] = "Wabash";
	// char word[] = "aba";
	// cout << strstr(river, word) << endl;

	// cout << strstr(s,u) << endl; 
	const char *u = "Alexandra";
	cout <<  << endl;
}